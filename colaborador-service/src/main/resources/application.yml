server:
  port: 8082
spring:
    application:
        name: colaborador-service

eureka:
    client:
        serviceUrl:
            defaultZone: http://eureka-server:8761/eureka/
        registerWithEureka: true
        fetchRegistry: true
    instance:
        preferIpAddress: false
        hostname: ${HOSTNAME:colaborador-service}